<!DOCTYPE html>
<html>
  <head>
    <title>MyMapNow.com</title>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <%- if protect_against_forgery? -%>
      <meta name="authenticity-token" id="authenticity-token" content="<%= form_authenticity_token %>">
    <%- end -%>
    <%= stylesheet_link_tag 'dialog/jquery-ui-1.7.2.custom.css', 'layout', :cache => true %>
    <!--[if IE]>
    <%= javascript_include_tag 'html5' %>
    <![endif]-->
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
    <script src="http://j.maxmind.com/app/geoip.js"></script>
    <script src="http://simile.mit.edu/timeline/api/timeline-api.js" type="text/javascript"></script>
    <%= javascript_include_tag 'jquery-1.3.2.min', 'jquery-ui-1.7.2.custom.min', 'perfecttime/PerfectTime', 'jquery.form', :cache => true %>
    <%= javascript_include_tag 'timeline', 'timeline_theme' %>
    <%= javascript_include_tag 'geoip', 'old_map', 'map' %>
    <%= javascript_include_tag 'items' %>
    <script type="text/javascript">
    <!--
    function submitenter(myfield,e)
    {
    var keycode;
    if (window.event) keycode = window.event.keyCode;
    else if (e) keycode = e.which;
    else return true;

    if (keycode == 13)
       {
       Map.search($('#search-box').val());
       return false;
       }
    else
       return true;
    }
    //-->
    </script>
  </head>
  <body>
    <wrapper>
      <search>
        <button onclick="Map.whatsMyLocation();">&nbsp;</button>
        <input type="text" id="search-box" name="search-box" value=" " onKeyPress="return submitenter(this,event)" />
      </search>      
      <sidebar-wrapper>
        <sidebar-header>
          <img src="/images/mymapnow_logo.png" />
        </sidebar-header>
        <aside>
          <ol></ol>
        </aside> 
        <div id="copyright">
          <a href="/terms.html">Terms of Use</a>
        </div>
        <sidebar-footer> 
        <div id="spacer">
          <%= link_to('<span>Add Event</span>', new_item_path, :id => 'add-event') %>
        </div>
        </sidebar-footer>
      </sidebar-wrapper>
      <div>
        <%= yield %>
      </div>
    </wrapper>
  </body>
</html>